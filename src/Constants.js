var baseUrl = "https://b4tov86un0.execute-api.us-east-2.amazonaws.com/dev"
window.localStorage.setItem('baseUrl', baseUrl);

const Constants = {
    BASEURL: baseUrl,
    AUTHToken :" ",
    JSON : {
        "ACCESSTOKEN" : "access_token"
    }
  }

export default Constants